# 测试文档

## 测试目标

测试重点是确保小说输入可以被稳定解析，并且输出 YAML 符合 Schema，便于作者继续编辑和后续工具处理。

## 测试范围

- 章节数量校验。
- 章节解析准确性。
- AI 输出结构完整性。
- YAML 语法校验。
- YAML Schema 校验。
- 前端输入、AI Provider 状态展示、模型选择、文件导入、生成、YAML 展示、结构化预览、标题/logline/场景基础信息/beats 编辑、同步状态、校验、复制和导出流程。

## 初步测试用例

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| T001 | 输入少于 3 个章节 | 返回章节数量不足提示 |
| T002 | 输入 3 个标准章节 | 成功生成 YAML |
| T003 | 输入包含大量心理描写 | 转换为动作、旁白或对白 |
| T004 | AI 输出缺少必填字段 | 自动追加一次修复提示；修复后通过则返回 YAML，仍失败则返回校验路径 |
| T005 | YAML 中包含非法结构 | 返回 Schema 校验错误 |
| T006 | 复制编辑后的 YAML | 剪贴板内容等于当前 YAML |
| T007 | 导入本地 `.txt` 或 `.md` 文件 | 小说文本区自动填入文件内容 |
| T008 | 生成示例 YAML 后查看结构化预览 | 显示剧本标题、logline、人物、场景和 beats |
| T009 | 在结构化预览中编辑标题和 logline | YAML 自动更新为最新标题和 logline |
| T010 | 在结构化预览中编辑场景标题、地点和时间 | YAML 自动更新为最新场景基础信息 |
| T011 | 在结构化预览中编辑、新增、删除 beat | YAML 自动更新为最新 beats 列表 |
| T012 | 结构化编辑后查看同步状态 | 页面提示结构化修改已同步到 YAML |
| T013 | 下载生成后的 YAML | 浏览器下载 `script-draft.yaml`，文件内容包含当前 YAML |
| T014 | 在结构化预览中编辑对白 beat 的说话人 | YAML 自动更新为最新说话人 |
| T015 | 在结构化预览中编辑、新增、删除人物 | YAML 自动更新为最新人物列表 |
| T016 | 在结构化预览中编辑、新增、删除场景登场人物 | YAML 自动更新为最新登场人物列表 |
| T017 | 在结构化预览中新增、删除场景 | YAML 自动更新为最新场景列表，且至少保留一个场景 |
| T018 | 打开页面查看 AI Provider 状态 | 显示本地骨架或远程 Provider 配置状态，且不显示密钥 |
| T019 | 输入英文或中文小说并使用远程 AI Provider 生成 JSON 风格 YAML key | 返回内容会规范化为项目统一 YAML key 样式，且结构化预览可以识别 `script:` 根节点 |
| T020 | 打开模型下拉 | 显示本地骨架、DeepSeek-V4-Pro、Kimi-2.6 和 GLM-4.7-FlashX，并对缺少 API Key 的远程模型提示未配置 |
| T021 | 生成接口传入 `model_id` | 后端按指定模型创建 Provider；非法 `model_id` 返回 `INVALID_INPUT` |

## 已覆盖的自动化测试

- 输入少于 3 个章节时，章节解析模块返回 `INVALID_CHAPTER_COUNT`。
- 输入 3 个标准章节时，章节解析模块返回 3 个章节对象。
- 支持 `第 1 章`、`第一章`、`第001章`、Markdown 标题、`第1回`、`Chapter 1`、`1. 初遇` 等常见章节标题。
- 正文中的章节字样不会被误判为章节标题。
- 空文本、无法识别章节标题或章节正文为空时，章节解析模块返回 `INVALID_INPUT`。
- 章节解析结果可以转换为剧本数据模型，并序列化为包含 `script`、`source`、`characters`、`scenes` 和 `beats` 的 YAML 初稿。
- `POST /api/scripts/generate` 可以把 3 章以上小说文本转换为 YAML 初稿，并返回 `status`、`schema_version` 和 `yaml`。
- `POST /api/scripts/generate` 支持通过 `model_id` 指定本地骨架、DeepSeek-V4-Pro、Kimi-2.6 或 GLM-4.7-FlashX，并会拒绝不支持的模型 id。
- `POST /api/scripts/generate` 会在章节不足、输入格式不可识别或输出格式不支持时返回明确错误码。
- 前端可以调用 `POST /api/scripts/generate`，并将返回的 YAML 填入右侧预览、编辑和下载区域。
- 前端可以将当前 YAML 映射为结构化预览，展示剧本标题、logline、人物、场景和 beats。
- 前端可以在结构化视图中编辑剧本标题和 logline，并将结果写回当前 YAML。
- 前端可以在结构化视图中编辑场景标题、地点和时间，并将结果写回当前 YAML。
- 前端可以在结构化视图中编辑 beat 类型和文本，并支持新增、删除 beat。
- 前端可以在结构化编辑后显示同步状态，并让校验、复制和下载流程复用最新 YAML。
- 前端可以调用 `POST /api/scripts/validate`，并展示校验通过状态、错误路径和错误信息。
- `POST /api/scripts/validate` 可以校验后端生成的 YAML，并对语法错误、缺失字段和非法 beat 类型返回 `YAML_VALIDATION_FAILED`。
- AI Provider 默认使用 `local` 模式，后端测试不会访问外部服务。
- 配置文件加载已覆盖 `.env` 风格键值读取、引号解析、`AI_CONFIG_FILE` 指定路径，以及环境变量优先级。
- 后端启动器已覆盖 `python main.py -p <port>`、`--frontend-port`、`--reload` 参数解析，以及启动前写入 CORS 所需前端端口。
- 本地演示停止脚本已通过 PowerShell 解析器语法检查，并用 `-DryRun` 验证可识别当前项目的后端、前端开发服务器和相关子进程。
- OpenAI-compatible Provider 已覆盖配置读取、Markdown YAML 代码块提取、强化提示词约束、Schema 校验失败、一次修复重试和接口错误映射测试。
- OpenAI-compatible Provider 已覆盖从带说明文字或 Markdown 代码块的 AI 回复中提取真实 `script:` YAML、修复英文智能标点 mojibake，以及清理残留 C1 控制字符。
- OpenAI-compatible Provider 已覆盖远程 AI 返回 JSON 风格 YAML key（例如 `"script":`）时的规范化输出，确保最终响应使用前端结构化预览可识别的 `script:` 根节点。
- 英文 `Chapter N` 三章样例已通过远程 AI Provider 实测，验证生成结果可通过 Schema 校验、包含 3 个场景，并且不含常见英文乱码标记。
- `GET /api/ai/status` 已覆盖默认 local 状态、DeepSeek 配置状态和密钥不泄露断言。
- `GET /api/ai/models` 已覆盖本地骨架、DeepSeek-V4-Pro、Kimi-2.6、GLM-4.7-FlashX 模型列表、缺失 API Key 提示和密钥不泄露断言。
- 前端 E2E 烟测会显式以 `AI_PROVIDER=local` 启动后端，避免个人 `.env` 影响自动化结果，并覆盖顶部 AI/后端状态展示、模型下拉默认本地骨架、后端连接状态和 YAML 面板不再展示 schema 版本徽标。
- `docs/examples/rain-letter-novel.txt` 示例文本可以生成符合 `docs/examples/rain-letter-script.yaml` 结构的 YAML 骨架。
- 示例文本通过生成接口返回的 YAML 必须通过后端 Schema 校验。
- 前端端到端烟测脚本 `frontend` 目录下的 `npm run smoke:e2e` 会通过 `python main.py -p <port>` 启动或复用后端与前端，并通过项目级 Playwright 启动真实 Chromium 浏览器。
- 浏览器 E2E 已覆盖打开页面、AI Provider 状态展示、导入 `docs/examples/rain-letter-novel.txt`、生成 YAML、结构化预览渲染、编辑标题、logline、场景基础信息和 beats、增删 beat、同步状态提示、Schema 校验通过、复制 YAML 到剪贴板和下载 `script-draft.yaml`。
- 浏览器 E2E 已覆盖将生成结果临时改写为 `"script":`、`"title":` 等 JSON 风格 YAML key 后，前端会恢复为 `script:` 样式并继续渲染结构化预览。
- Playwright 不再是可选 fallback；若 `frontend` 目录未安装依赖或未安装 Chromium，烟测会失败并提示先运行 `npm install` 与 `npm run e2e:install`。

## 待定事项

- 是否将 `npm run e2e:install` 和 `npm run smoke:e2e` 纳入 CI 待定。
